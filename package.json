{
  "name": "demopolly",
  "version": "1.0.0",
  "description": "A Poll Destination",
  "main": "index.js",
  "private": true,
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "dev": "concurrently \"npm:dev:api\" \"npm:dev:ui\"",
    "dev:api": "cd apps/api && npm run start:dev",
    "dev:ui": "cd apps/ui && npm run dev"
  },
  "author": "",
  "license": "ISC",
  "workspaces": [
    "apps/*"
  ],
  "dependencies": {
    "concurrently": "^9.2.0"
  }
}
